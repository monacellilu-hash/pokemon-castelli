# Cartella di memoria di Claude Code (non fa parte del gioco)
.claude/

# File di sviluppo non necessari per il deploy
Old Versioning Instruction/
LAST UPDATE.txt

# File di sistema Windows
Thumbs.db
desktop.ini

# File macOS
.DS_Store

# Editor
.vscode/
*.swp
*.tmp

# Asset originali (pack completo — solo i file specifici vengono committati)
sprites/Essentials FRLG/
Essentials FRLG/

# Dipendenze npm (Playwright ecc.: non fanno parte del gioco)
node_modules/

# Screenshot di test (Playwright)
test-*.png

# File di sessione/progetto di Tiled (non servono al gioco)
*.tiled-session
*.tiled-project

# Cartella di scarti asset (non referenziata dal gioco, ~35MB)
sprites/NO/

# File orfani spostati qui da un censimento (non referenziati da nessuna
# mappa/codice, sess. 21 settembre 2026) — mai committati
non_utilizzati/

# Materiale grezzo/di riferimento, non caricato dal gioco (censimento
# sess. 21 settembre 2026): sprite NPC originali già convertiti in
# "sprites/npc e trainer per claude/", tileset RPG Maker campione mai usato,
# script Ruby estratti da Essentials solo per consultazione
OW_Trainer_sprite/
free/
strumenti/scripts_estratti/
